package zcl

import (
	"testing"
)

func TestDescribeValues(t *testing.T) {
	/*for _, fn := range Commands {
		cmd := fn()
		t.Run(cmd.Name(), func(t *testing.T) {

			values := DescribeValues(cmd)
			log.Printf("%+v", values)

		})
	}*/
}
